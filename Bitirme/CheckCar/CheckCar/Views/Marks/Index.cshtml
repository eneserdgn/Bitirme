@model IEnumerable<CheckCar.Models.Mark>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MarkName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MarkImage)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <img src="~/Image/MarkImage/@item.MarkImage" width="70" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MarkName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MarkImage)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ID })
            </td>
        </tr>
    }

</table>
